Para compilar e executar o programa, abrir o arquivo bcp(projeto) no codeblocks
Padrao de escrita utilizado foi o CamelCase, em que nas variaveis a primeira letra é minuscula, 
nas funcoes a primeira letra é maiuscula, e nas constantes tudas as letras sao maiusculas e separadas por underline(_)